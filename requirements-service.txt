# Service layer only. The library and every experiment in this repo run without
# any of these -- see requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Optional backends. Each degrades to in-memory or no-op when absent:
#   redis            -> constraints and posterior snapshots stay in process
#   confluent-kafka  -> outcome events are dropped
# ClickHouse needs no client library; the sink posts to its HTTP interface.
redis>=5.0
confluent-kafka>=2.3
